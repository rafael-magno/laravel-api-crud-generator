<?php

namespace {{ namespace }};

use {{ namespaceRepository }}\{{ table.name|singularStudly }}Repository;
use {{ namespacePaginateRequest }}\PaginateRequest;

class List{{ table.name|studly }}Action
{
    public function __invoke(
        {{ table.name|singularStudly }}Repository $repository,
        PaginateRequest $request
    ) {
        return $repository->paginate(
            $request->get('per_page')
        );
    }
}

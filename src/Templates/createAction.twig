<?php

namespace {{ namespace }};

use {{ namespaceRepository }}\{{ table.name|singularStudly }}Repository;
use {{ namespaceSaveRequest }}\Save{{ table.name|singularStudly }}Request;

class Create{{ table.name|singularStudly }}Action
{
    public function __invoke(
        {{ table.name|singularStudly }}Repository $repository,
        Save{{ table.name|singularStudly }}Request $request
    ) {
        return $repository->create(
            $request->all()
        );
    }
}
